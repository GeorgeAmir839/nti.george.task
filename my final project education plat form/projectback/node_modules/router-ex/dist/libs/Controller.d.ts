import { Request, Response } from 'express';
export default class Controller {
    request: Request;
    response: Response;
    constructor(req: Request, res: Response);
    respond(status: number, data: any): Response<any>;
    respondSuccess(data?: any): Response<any>;
    respondError(errorCode: number, message?: String): Response<any>;
    respondFile(file: string): void;
}
