import IMiddleware from './Middleware';
export default class MiddlewareService {
    private static middlewares;
    static register(name: string, middleware: IMiddleware): typeof MiddlewareService;
    static get(name: string): CallableFunction;
}
