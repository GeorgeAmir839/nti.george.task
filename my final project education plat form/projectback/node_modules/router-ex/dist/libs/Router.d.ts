import Route from './Route';
import Controller from './Controller';
export interface Action {
    0: typeof Controller;
    1: string;
}
export interface IRouterOptions {
    middlewares?: Array<Function>;
    baseUrl?: string;
}
export default class Router {
    app: any;
    expressRouter: any;
    options: IRouterOptions;
    routes: Array<Route>;
    constructor(app: any, options?: IRouterOptions);
    register(app: any): void;
    private addRoute;
    resource(path: string, controller: typeof Controller): Route[];
    post(path: string, action: Action): Route;
    get(path: string, action: Action): Route;
    put(path: string, action: Action): Route;
    patch(path: string, action: Action): Route;
    delete(path: string, action: Action): Route;
}
