"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var MiddlewareService = (function () {
    function MiddlewareService() {
    }
    MiddlewareService.register = function (name, middleware) {
        this.middlewares[name] = middleware;
        return this;
    };
    MiddlewareService.get = function (name) {
        if (!this.middlewares[name]) {
            throw Error("The middleware with key \"" + name + "\" is not registered");
        }
        return this.middlewares[name].handler;
    };
    MiddlewareService.middlewares = {};
    return MiddlewareService;
}());
exports.default = MiddlewareService;
